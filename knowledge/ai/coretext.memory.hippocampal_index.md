## Summary
Analyzed the Coretext architecture against cognitive science models of human memory. Validated that Coretext's background Hebbian injection mimics spreading activation, and progressive disclosure of node pointers versus full content aligns with biological activation thresholds. Verified that combining vector similarity with Hebbian graphs mirrors the brain's multimodal integration and hybrid search. Established that the memory cold start problem is solved biologically via pattern completion (semantic vector fallback) and top-down control (strategic LLM subagent probing). Formulated the "Golden Rule" for the `experience.json` event log based on Hippocampal Indexing Theory: it must only store relational indices (files touched, git commit hash, session ID) to bind "what, where, and when," rather than duplicating semantic content already retrievable via Git or the filesystem.

## Related Notes
- [[coretext]] - Core concept being evaluated.
- [[coretext.memory.background_intuition|Coretext as Background Intuition over GitNexus and Native Search]] - Referenced for the architectural shift to injection cues.
- [[coretext.memory.hebbian_experience|The Native Primitives and Hebbian Experience Architecture]] - Referenced for the transition to native file storage and `experience.json` logging.
- [[SurrealDB]] - Discussed in the context of hybrid search queries.

## Original Prompts Reference
- "is my current idea for coretext as close as possible how human use long term memory on tasks, which usually require some kind of working memory (like context memory) but connected to long term memory? is injecting the name of the long term context nodes similar to how human brain bring out long term knowledge?"
- "make sense. this act just like progressive disclosure of the nested instruction files like agents.md or claude.md, also match how agent skill is, with the agent quickly access the yaml frontmatter with drscription, and only read the whole instruction of the skill when necessary? also, the daemon should run on comparing vector similarity or finding connections on graph or both? that's where we can harness the power of surrealdb hybrid query? does the brain has that kind of hybrid query, like multiple senses? also, by doing this, we can also inject pdf files, images, diagrams, etc to the agent too? or just find connections to read or editted files only? im yet to imagine how the daemon would find the important keywords to run grep, if there is not yet many connections. or should i use a subagent that constantly read the conversation turn and run grep search to find relevant snippets?"
- "how about the file-based event log, that will be used to save turn/session/events that eventually will be loaded in to surrealdb graph, what should it store? git commits, tool call, llm api call, response etc? or just record when an edge is formed?"
- "that golden rule, especially "if you can retrieve..." is key. now save a summary of this discussion."