# Hooks
search for <hook_context>

# Log
- using [[sandbox]] mode in another directory (standard MacOS Setbelt)
- the security subagents keeps restricting tool use. currently avoid using that subagent. fixed for now with deleting settings.json