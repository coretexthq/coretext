# Formal Validation Contract Matrix

Status values in this matrix are methodological defaults. They remain `NOT_RUN` until fixture execution and evidence comparison occur.

| ID | Contract | Fixture Inputs To Freeze | Data To Collect | Expected Evidence Artifacts | Pass Criterion | Failure Classes | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| FV-01.1 | Route selection returns an ordered edge list for a fixed path, action, and ledger. | `F-ROUTE-001` ledger; normalized path/action cases. | Input path, action, matched edge records, ledger line order. | `expected/route-selection.json`; `actual/route-selection.json`. | Exact ordered list equals expected `R_E(p, a)`. | Coretext mechanism, knowledge maintenance. | NOT_RUN |
| FV-01.2 | Route matching respects literal, glob, and directory-prefix semantics. | `F-ROUTE-001` ledger with literal, wildcard, and directory-prefix sources. | Match predicate result per edge. | `expected/route-match-predicates.csv`; `actual/route-match-predicates.csv`. | Every predicate agrees with fixture expectation. | Coretext mechanism, knowledge maintenance. | NOT_RUN |
| FV-01.3 | Hook filtering respects `read`, `write`, and `both`. | `F-ROUTE-001` ledger with all hook filters. | Action, hook value, active/inactive decision. | `expected/hook-filtering.csv`; `actual/hook-filtering.csv`. | No inactive edge is returned and no active edge is omitted. | Coretext mechanism. | NOT_RUN |
| FV-02.1 | Context rendering separates route selection from rendering. | `F-ROUTE-001`; `F-RENDER-001` target files and directories. | Selected edges before render; rendered hints; rendered full files. | `expected/context-rendering.json`; `actual/context-rendering.json`. | Selected edge list is unchanged by missing, binary, or unreadable render targets. | Coretext mechanism, knowledge maintenance. | NOT_RUN |
| FV-02.2 | `hint` routes render compact descriptions and target paths. | Hint edge targets, including file and directory targets. | Hint lines and optional directory child inventory. | `expected/hints.txt`; `actual/hints.txt`. | Hints include description and target path without hydrating full target content. | Coretext mechanism, knowledge maintenance. | NOT_RUN |
| FV-02.3 | `full` routes hydrate readable target content. | Full edge targets: one file and one directory. | Rendered full payload and target inventory. | `expected/full-files.txt`; `actual/full-files.txt`. | Readable targets appear in full payload; binary handling is documented. | Coretext mechanism, knowledge maintenance. | NOT_RUN |
| FV-03.1 | Runtime adapter normalizes Codex read payloads with stable paths. | `F-RUNTIME-CODEX-READ-001`. | Runtime, event, tool, action, session ID, normalized paths. | `expected/codex-read-normalized.json`; `actual/codex-read-normalized.json`. | Common request shape matches expected fields. | Coretext mechanism. | NOT_RUN |
| FV-03.2 | Runtime adapter normalizes Codex `apply_patch` write payload paths. | `F-RUNTIME-CODEX-WRITE-001`. | Parsed patch paths, action, session ID, sanitized state filename. | `expected/codex-write-normalized.json`; `actual/codex-write-normalized.json`. | All patch file headers normalize to project-relative paths. | Coretext mechanism. | NOT_RUN |
| FV-03.3 | Runtime adapter normalizes Antigravity write and lineage payloads. | `F-RUNTIME-ANTIGRAVITY-001`; `F-RUNTIME-ANTIGRAVITY-LINEAGE-001`. | Event, tool input, action, session ID, paths, queued lineage state. | `expected/antigravity-normalized.json`; `actual/antigravity-normalized.json`. | Supported payloads normalize and runtime-specific delivery boundary is preserved. | Coretext mechanism. | NOT_RUN |
| FV-03.4 | Unsupported or malformed payloads fail open. | `F-RUNTIME-UNSUPPORTED-001`. | No-op or allow response; absence of state mutation. | `expected/unsupported-response.json`; `actual/unsupported-response.json`. | No context is injected and no action is blocked. | Coretext mechanism. | NOT_RUN |
| FV-04.1 | Write gate allows when no write context matches. | `F-GATE-NO-MATCH-001`. | Payload, rendered context, response, acknowledgement state. | `actual/write-gate-no-match.json`. | Response allows and acknowledgement store is unchanged. | Coretext mechanism, knowledge maintenance. | NOT_RUN |
| FV-04.2 | First matching write denies and records requested paths. | `F-GATE-FIRST-WRITE-001`. | Pre-ack state, deny response, rendered context, post-ack state. | `expected/write-gate-deny.json`; `actual/write-gate-deny.json`. | First write is denied and requested normalized paths are recorded for the session. | Coretext mechanism. | NOT_RUN |
| FV-04.3 | Retry in same session allows. | `F-GATE-RETRY-001`. | Ack state before retry, allow response, ack state after retry. | `actual/write-gate-retry.json`. | Retry allows only because all requested paths are acknowledged in that session. | Coretext mechanism. | NOT_RUN |
| FV-04.4 | Acknowledgement is session-isolated. | `F-GATE-SESSION-ISOLATION-001`. | Two session IDs and independent ack stores. | `actual/write-gate-session-isolation.json`. | Ack in one session does not allow first write in another session. | Coretext mechanism. | NOT_RUN |
| FV-04.5 | Hook errors fail open without corrupting ledger state. | `F-GATE-FAIL-OPEN-001`. | Simulated error, response, ledger hash before and after. | `actual/write-gate-fail-open.json`. | Action is allowed or no-op and authoritative ledger is unchanged. | Coretext mechanism. | NOT_RUN |
| FV-05.1 | Dotted durable notes map to project, scope, and virtual scope nodes. | `F-HIERARCHY-001` note tree. | Expected hierarchy JSON; actual hierarchy JSON. | `expected/hierarchy-tree.json`; `actual/hierarchy-tree.json`. | Node types, names, paths, and virtual flags match expected hierarchy. | Coretext mechanism, knowledge maintenance. | NOT_RUN |
| FV-05.2 | Sessions attach to the longest existing durable prefix. | `F-HIERARCHY-SESSION-001`. | Session note path, owning durable prefix, actual parent node. | `expected/session-attachment.csv`; `actual/session-attachment.csv`. | Every session appears under the expected nearest durable owner. | Coretext mechanism, knowledge maintenance. | NOT_RUN |
| FV-05.3 | Archive and inactive nested knowledge paths are excluded. | `F-HIERARCHY-EXCLUSION-001`. | Candidate paths and inclusion/exclusion result. | `actual/hierarchy-exclusions.csv`. | No archive path participates in active hierarchy or lineage. | Coretext mechanism, knowledge maintenance. | NOT_RUN |
| FV-05.4 | Delegation follows direct child namespace and write-boundary protocol. | `F-DELEGATION-001` packaged instruction and sample child session. | Role mapping, session path, durable-note update ownership. | `actual/delegation-audit.csv`. | Child role maps to direct child namespace; parent reads child summary before durable distillation. | Agent behavior, knowledge maintenance. | NOT_RUN |
| FV-06.1 | Session summaries contain required reusable evidence fields. | `F-SESSION-REUSE-001` session summaries. | Goal, input context, actions, decisions, changed artifacts, verification, risks, deltas, handoff. | `actual/session-summary-audit.csv`. | All required fields are present or explicitly not applicable. | Agent behavior, knowledge maintenance. | NOT_RUN |
| FV-06.2 | Durable-note promotion uses reviewed stable deltas, not raw chronology. | `F-PROMOTION-001` session plus durable note pair. | Session evidence, durable delta, reviewer trace. | `actual/durable-promotion-audit.csv`. | Durable note records only reusable current state and links evidence appropriately. | Agent behavior, knowledge maintenance. | NOT_RUN |
| FV-06.3 | Rule or graph promotion satisfies reuse, scope, future-facing, review, and verifiability. | `F-RULE-PROMOTION-001` optional rule/edge examples. | Promotion predicate fields; lint result if edge is claimed. | `actual/promotion-predicate-audit.csv`; optional `actual/graph-lint.txt`. | Every promoted constraint satisfies all predicate fields and has validation evidence. | Agent behavior, knowledge maintenance, Coretext mechanism. | NOT_RUN |
| FV-07.1 | Telemetry records normalize file activity into session JSONL. | `F-DASH-SESSION-001` telemetry fixtures. | `node_id`, `timestamp`, `tool_name`, `runtime`, optional action. | `actual/session-telemetry-audit.csv`. | Every record is project-relative and maps to a known repository node or documented missing node. | Coretext mechanism, knowledge maintenance. | NOT_RUN |
| FV-07.2 | Session labels map telemetry session IDs to `knowledge/ai` frontmatter conversations. | `F-DASH-LABEL-001` telemetry plus summary frontmatter. | Conversation ID, summary basename, mapped label. | `expected/dashboard-sessions.json`; `actual/dashboard-sessions.json`. | Human-readable labels derive from summary frontmatter, not dashboard-only state. | Coretext mechanism, knowledge maintenance. | NOT_RUN |
| FV-07.3 | Read/write dashboard highlights trace to telemetry tool names. | `F-DASH-HIGHLIGHT-001`. | Tool names, inferred read/write action, highlighted nodes. | `expected/dashboard-highlights.json`; `actual/dashboard-highlights.json`. | Read and write highlights match telemetry and write precedence is documented. | Coretext mechanism, knowledge maintenance. | NOT_RUN |
| FV-07.4 | Dashboard and hierarchy outputs are derived views. | `F-DASH-DERIVED-001`. | Source fixture hashes before and after dashboard/kernel calls. | `actual/derived-state-hash-check.txt`. | Dashboard/API/kernel inspection does not mutate authoritative fixtures. | Coretext mechanism. | NOT_RUN |
